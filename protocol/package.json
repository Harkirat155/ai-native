{
  "name": "@ai-native/vscode-bridge-protocol",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "node ./tools/generate.mjs",
    "generate:python": "node ./tools/generate-python-sdk.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint yet)\""
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
